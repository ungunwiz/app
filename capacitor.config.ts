import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.ungunwiz.net',
  appName: 'ungunwiz',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
