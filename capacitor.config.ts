import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.ungunwiz.gg',
  appName: 'ungunwiz',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
